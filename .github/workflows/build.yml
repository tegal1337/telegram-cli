name: Build

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
    branches: [main]

jobs:
  build-linux:
    name: Linux (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-24.04, ubuntu-22.04]

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.25'

      - name: Cache TDLib
        id: cache-tdlib
        uses: actions/cache@v4
        with:
          path: ~/td/tdlib
          key: tdlib-${{ matrix.os }}-v1.8.43

      - name: Setup TDLib
        if: steps.cache-tdlib.outputs.cache-hit != 'true'
        run: ./scripts/setup-tdlib.sh

      - name: Build
        run: make build

      - uses: actions/upload-artifact@v4
        with:
          name: tele-tui-linux-amd64-${{ matrix.os }}
          path: bin/tele-tui

  build-macos:
    name: macOS (${{ matrix.arch }})
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: macos-14
            arch: arm64
          - runner: macos-13
            arch: x86_64

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.25'

      - name: Setup TDLib
        run: brew install tdlib

      - name: Build
        run: make build

      - uses: actions/upload-artifact@v4
        with:
          name: tele-tui-darwin-${{ matrix.arch }}
          path: bin/tele-tui

  build-windows:
    name: Windows (x86_64)
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.25'

      - name: Setup MSYS2
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          update: true
          install: >-
            mingw-w64-x86_64-gcc
            mingw-w64-x86_64-cmake
            mingw-w64-x86_64-gperf
            mingw-w64-x86_64-openssl
            mingw-w64-x86_64-zlib
            make git

      - name: Cache TDLib
        id: cache-tdlib-win
        uses: actions/cache@v4
        with:
          path: C:/td/tdlib
          key: tdlib-windows-mingw64-v1.8.43

      - name: Build TDLib
        if: steps.cache-tdlib-win.outputs.cache-hit != 'true'
        shell: msys2 {0}
        run: |
          git clone --depth 1 https://github.com/tdlib/td.git /c/td/td-src
          cd /c/td/td-src && mkdir build && cd build
          cmake -G "MSYS Makefiles" -DCMAKE_BUILD_TYPE=Release -DCMAKE_INSTALL_PREFIX=/c/td/tdlib ..
          cmake --build . -j$(nproc)
          cmake --install .

      - name: Build
        shell: msys2 {0}
        env:
          GOROOT: ${{ env.GOROOT_1_25_X64 }}
        run: |
          export PATH="$GOROOT/bin:$PATH"
          CGO_ENABLED=1 \
          CGO_CFLAGS="-IC:/td/tdlib/include" \
          CGO_LDFLAGS="-LC:/td/tdlib/bin -ltdjson" \
          go build -trimpath -ldflags="-s -w" -o bin/tele-tui.exe ./cmd/teletui

      - uses: actions/upload-artifact@v4
        with:
          name: tele-tui-windows-amd64
          path: bin/tele-tui.exe

  release:
    name: Release
    needs: [build-linux, build-macos, build-windows]
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Package
        run: |
          mkdir release
          for dir in artifacts/tele-tui-linux-*; do
            name=$(basename "$dir"); cp "$dir/tele-tui" "release/${name}"; chmod +x "release/${name}"
          done
          for dir in artifacts/tele-tui-darwin-*; do
            name=$(basename "$dir"); cp "$dir/tele-tui" "release/${name}"; chmod +x "release/${name}"
          done
          cp artifacts/tele-tui-windows-amd64/tele-tui.exe release/tele-tui-windows-amd64.exe

      - uses: softprops/action-gh-release@v2
        with:
          files: release/*
          generate_release_notes: true
